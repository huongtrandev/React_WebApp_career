let greet = "xin chao viet nam";
const LESSON = "Su dung module tron ES6";
function greeting(name, content) {
  return name + " " + content;
}
// xuất các biến và hàm

export { greet, LESSON, greeting };
